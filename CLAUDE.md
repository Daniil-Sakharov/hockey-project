# StarRink Project Guide

**[ВАЖНО!!!] Всегда отвечай на русском языке**

---

## 🎯 Основные принципы проекта

### Архитектура
- **Backend**: Go с модульным монолитом
- **iOS**: SwiftUI + MVVM + Clean Architecture
- **Frontend**: React + TypeScript + Feature-Sliced Design
- **База данных**: PostgreSQL с Goose миграциями
- **DI контейнер**: Wire для управления зависимостями


## 📁 КРИТИЧЕСКИЕ ПРАВИЛА СТРУКТУРЫ ФАЙЛОВ

### 🎯 Золотое правило размера файлов

| Размер | Статус |
|--------|--------|
| **50-100 строк** | ✅ ИДЕАЛЬНО |
| **100-150 строк** | ⚠️ Приемлемо |
| **150+ строк** | ❌ ОБЯЗАТЕЛЬНО разделить |
| **200+ строк** | 🚫 НИКОГДА |

### 📂 Правило максимум 7 файлов в папке

**КРИТИЧЕСКИ ВАЖНО:**
- ❌ **НЕ БОЛЕЕ 7 файлов в одной папке**
- ✅ **Если файлов становится больше → создавай логические подпапки**
- ✅ **Группируй по функциональности, а не по типу**

**Пример правильной структуры:**
```
internal/modules/ads/adapter/
├── converters/          # Подпапка для конвертеров
│   ├── tyver.go        # 80 строк
│   ├── meta.go         # 90 строк
│   └── pipiads.go      # 70 строк
├── filters/            # Подпапка для фильтров
│   ├── ad_filters.go   # 100 строк
│   └── search.go       # 85 строк
├── repository.go       # 120 строк
└── gateway.go          # 95 строк
```

**Пример НЕПРАВИЛЬНОЙ структуры (избегать!):**
```
internal/modules/ads/adapter/
├── ad_filters.go
├── ad_helpers.go
├── ad_null_helpers.go
├── ad_p2_save.go
├── ad_upsert.go
├── quick_scraper_convert.go
├── tyver_converter.go
├── meta_converter.go
├── pipiads_converter.go   # ❌ Уже 9 файлов! Нужно разделить!
```

### Принципы разделения файлов

1. **Один файл = одна ответственность**
2. **Логическое группирование функций**
3. **Легкость навигации и поиска**
4. **Простота тестирования**
5. **Удобство code review**

---

## 🧹 ПРАВИЛО ЧИСТОТЫ ПРОЕКТА

### ❌ НЕ создавать мусорку!

**КРИТИЧЕСКИ ВАЖНО:**
- ❌ **НЕ оставлять временные тестовые скрипты**
- ❌ **НЕ создавать одноразовые файлы для проверки**
- ❌ **НЕ оставлять закомментированный код**
- ✅ **ВСЕГДА удалять временные файлы после использования**

**Примеры мусорки (УДАЛЯТЬ!):**
```go
// test.go
// quick_test.go
// debug_script.go
// tmp_converter.go
// check_something.go
```

**Что делать с временными скриптами:**
1. Если создал для тестирования → удали сразу после проверки
2. Если нужен реальный тест → создай `*_test.go` с table-driven tests
3. Если нужна утилита → создай в `cmd/` с нормальным названием

**Исключения (можно оставить):**
- `cmd/import-tyver/` - полноценная утилита для импорта
- Реальные unit тесты `*_test.go`
- Миграции в `migrations/`

---

## 🏛️ Архитектурные принципы

### Модульный монолит
- Четкое разделение модулей
- Минимальные зависимости между модулями
- Shared компоненты для общей функциональности
- DI контейнер для управления зависимостями

### Слои модуля
```
internal/modules/[module]/
├── domain/          # Бизнес-логика, интерфейсы
├── usecase/         # Use cases, orchestration
├── adapter/         # Реализация интерфейсов (DB, external APIs)
└── transport/       # HTTP handlers, middleware
```

### Качество кода
- Понятные имена переменных и функций
- Комментарии для сложной логики
- Обработка ошибок на всех уровнях
- Unit тесты рядом с кодом (`*_test.go`)

---

## 📝 Именование

### Файлы
- `snake_case.go` для Go файлов
- Суффикс `_test.go` для тестов
- Понятные имена, отражающие содержимое

### Переменные и функции
- `camelCase` для приватных
- `PascalCase` для публичных
- Короткие имена для локальных переменных (`ctx`, `err`, `tx`)
- Описательные имена для полей структур

### Пакеты
- Короткие, одно слово
- Без подчеркиваний
- НЕ использовать `common`, `utils`, `helpers`

---

## 🧪 Тестирование

### Принципы
- Unit тесты для критической логики
- Файл теста рядом с кодом: `service.go` → `service_test.go`
- Table-driven tests для множества кейсов
- Моки через интерфейсы

### Пример table-driven test
```go
func TestAdFilter(t *testing.T) {
    tests := []struct {
        name     string
        input    domain.Ad
        expected bool
    }{
        {"active ad", domain.Ad{Status: "active"}, true},
        {"paused ad", domain.Ad{Status: "paused"}, false},
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            result := Filter(tt.input)
            assert.Equal(t, tt.expected, result)
        })
    }
}
```

---

## 📝 Логирование

**ОБЯЗАТЕЛЬНО добавлять логирование везде:**

```go
// В начале каждой публичной функции
logger.Info(ctx, "Starting operation", zap.String("param", value))

// При ошибках
logger.Error(ctx, "Failed to do something", zap.Error(err))

// При успешном завершении важных операций
logger.Info(ctx, "Operation completed", zap.Int("count", result))

// Debug для отладки
logger.Debug(ctx, "Processing item", zap.Any("item", item))
```

**Что логировать:**
- Входящие HTTP запросы (middleware)
- Начало/конец usecase операций
- Ошибки с контекстом
- Важные бизнес-события
- Внешние вызовы (БД, Redis, внешние API)

---

## 📱 iOS / SwiftUI — ПРАВИЛА РАЗРАБОТКИ

### 🏛️ Архитектура: MVVM + Clean Architecture

**Слои приложения:**
```
StarRink/
├── App/                # Точка входа (@main), конфигурация
├── Core/               # Низкоуровневые сервисы
│   ├── Network/       # APIClient, APIEndpoints, APIError
│   └── Storage/       # KeychainManager, UserDefaults
├── Domain/             # Сущности и протоколы (бизнес-логика)
│   ├── Entities/      # User, Player, Team, Tournament...
│   └── Protocols/     # Repository протоколы
├── Data/               # Реализация данных
│   ├── DTOs/          # Data Transfer Objects (от API)
│   └── Repositories/  # Реализация протоколов из Domain
├── Features/           # Экраны по фичам (основной код)
│   └── [Feature]/
│       ├── Models/    # Модели данных фичи
│       ├── ViewModels/ # ViewModel'ы
│       └── Views/     # SwiftUI Views
├── Navigation/         # Навигация, роутинг, TabBar, Sidebar
└── UI/                 # Дизайн-система
    ├── Theme/         # AppColors, AppFonts, AppSpacing
    ├── Components/    # Переиспользуемые компоненты
    ├── Modifiers/     # ViewModifiers (GlassBackground...)
    └── Animations/    # Анимации (Splash, Aurora...)
```

### 🎯 Золотое правило размера файлов (iOS)

| Размер | Статус |
|--------|--------|
| **50-120 строк** | ✅ ИДЕАЛЬНО |
| **120-180 строк** | ⚠️ Приемлемо |
| **180+ строк** | ❌ ОБЯЗАТЕЛЬНО разделить |
| **250+ строк** | 🚫 НИКОГДА |

**SwiftUI View раздуваются быстро, поэтому:**
- ❌ НЕ класть несколько View в один файл
- ✅ Один View = один файл
- ✅ Вспомогательные компоненты (Row, Card, Section) → отдельные файлы
- ✅ Enum'ы навигации → отдельно от View
- ✅ ButtonStyle, ViewModifier → отдельные файлы

### 📂 Правило максимум 7 файлов в папке (iOS)

Те же правила что и для бэка:
- ❌ **НЕ БОЛЕЕ 7 файлов в одной папке**
- ✅ **Если больше → создавай подпапки**

### 📝 Именование (iOS)

**Файлы:**
- `PascalCase.swift` — имя файла = имя главного типа внутри
- Суффиксы: `*View.swift`, `*ViewModel.swift`, `*Repository.swift`, `*Manager.swift`

**Типы и переменные:**
- `PascalCase` для типов, View, ViewModel, Protocol
- `camelCase` для свойств, методов, локальных переменных
- `SCREAMING_SNAKE_CASE` НЕ используем (это не Swift-way)

**Примеры:**
```swift
// ✅ Правильно
struct PlayerProfileView: View { }
class StatsViewModel: ObservableObject { }
protocol AuthRepositoryProtocol { }

// ❌ Неправильно
struct playerProfile: View { }
class stats_view_model: ObservableObject { }
```

### 🎨 Дизайн-система (ОБЯЗАТЕЛЬНО)

**Цвета — ТОЛЬКО через AppColors:**
```swift
// ✅ Правильно
.foregroundColor(.srCyan)
.background(Color.srBackground)

// ❌ Неправильно
.foregroundColor(Color(hex: "#00D4FF"))
.background(Color(red: 0.05, green: 0.05, blue: 0.06))
```

**Шрифты — ТОЛЬКО через AppFonts:**
```swift
// ✅ Правильно
.font(.srHeading3)
.font(.srBody)

// ❌ Неправильно
.font(.system(size: 20, weight: .semibold))
.font(.title2)
```

**Отступы — ТОЛЬКО через AppSpacing:**
```swift
// ✅ Правильно
.padding(.horizontal, AppSpacing.screenHorizontal)
.padding(AppSpacing.md)

// ❌ Неправильно
.padding(.horizontal, 20)
.padding(16)
```

**Glass-эффекты — через модификатор:**
```swift
// ✅ Правильно
.glassCard()
.glassCard(padding: AppSpacing.md)

// ❌ Неправильно — не копировать glass-стили вручную
```

### 👤 Мульти-ролевая навигация

**Роли пользователей:**
```swift
enum UserRole: String, Codable {
    case fan       // Обычный пользователь / родитель
    case player    // Игрок
    case scout     // Скаут
    case coach     // Тренер
}
```

**Принципы:**
- `UserRole` определяет набор TabItem и NavigationItem (sidebar)
- Контейнер один (`MainContainerView`), контент переключается по роли
- Каждая роль имеет свой массив табов и sidebar-пунктов
- Общие экраны (Settings, Subscription) — shared между ролями
- НЕ создавать отдельный контейнер для каждой роли

**Пример:**
```swift
// ✅ Правильно — табы зависят от роли
static func tabs(for role: UserRole) -> [TabItem] {
    switch role {
    case .fan:    return [.home, .tournaments, .search, .settings]
    case .player: return [.home, .calendar, .profile, .settings]
    case .scout:  return [.home, .search, .watchlist, .settings]
    case .coach:  return [.home, .team, .calendar, .settings]
    }
}
```

### 🌐 Сетевой слой

**Принципы:**
- Все HTTP запросы через `APIClient` (singleton)
- Токены хранятся в `KeychainManager`
- DTO отдельно от Domain-моделей (маппинг через `init(from dto:)`)
- Repository через протокол для тестируемости
- async/await для всех сетевых вызовов

**Пример:**
```swift
// ✅ Правильно — протокол + реализация
protocol PlayerRepositoryProtocol {
    func getPlayer(id: String) async throws -> Player
}

class PlayerRepository: PlayerRepositoryProtocol {
    func getPlayer(id: String) async throws -> Player {
        let dto: PlayerDTO = try await APIClient.shared.request(
            endpoint: .player(id: id)
        )
        return Player(from: dto)
    }
}
```

### 🧪 Preview и тестирование (iOS)

**Preview — в каждом View:**
```swift
#Preview {
    MyView()
        .environmentObject(AuthViewModel())
}
```

**Mock-данные — через #if DEBUG:**
```swift
#if DEBUG
extension User {
    static let mock = User(
        id: "test-123",
        email: "test@starrink.ru",
        displayName: "Тестовый Пользователь",
        subscriptionTier: .free
    )
}
#endif
```

**ViewModel — тестируется через подмену репозитория:**
```swift
// В тестах подставляем mock repository
let mockRepo = MockAuthRepository()
let vm = AuthViewModel(repository: mockRepo)
```

### 🧹 Чистота проекта (iOS)

- ❌ НЕ оставлять `print()` — использовать `os.Logger` или убирать
- ❌ НЕ оставлять закомментированный код
- ❌ НЕ оставлять TODO без issue/задачи
- ✅ Unused imports — удалять
- ✅ Unused Views — удалять

### 📱 Команды iOS

```bash
# Сборка для симулятора
xcodebuild -project ios/StarRink/StarRink.xcodeproj \
  -scheme StarRink \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  build

# Запуск на симуляторе
xcrun simctl boot "iPhone 17 Pro"
open -a Simulator

# Установка и запуск
xcrun simctl install booted path/to/StarRink.app
xcrun simctl launch booted ru.starrink.app
```

---

## 🔧 Важные команды проекта

### Сборка
```bash
# Все бинарники в bin/
go build -o bin/bot ./cmd/bot
go build -o bin/scheduler ./cmd/scheduler
go build -o bin/scraper ./cmd/scraper
```

### Тестирование
```bash
# Все тесты
go test ./...

# Тесты конкретного модуля
go test ./internal/modules/scraper/...

# С покрытием
go test -cover ./...
```

### База данных
```bash
# Загрузить переменные окружения
source .env

# Миграции вверх
GOOSE_DBSTRING="postgres://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:$POSTGRES_PORT/$POSTGRES_DB?sslmode=disable" ~/go/bin/goose up

# Миграции вниз
~/go/bin/goose down

# Создать новую миграцию
~/go/bin/goose create [name] sql
```

### Docker
```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down

# Логи
docker-compose logs -f
```

### Окружение
```bash
# ВСЕГДА загружать .env перед работой с БД
source .env

# Проверка переменных
echo $POSTGRES_HOST
echo $TYVER_API_KEY
```

---

## 🔒 Безопасность

### ❌ НИКОГДА не комитить:
- `.env` файлы
- `credentials.json`
- API ключи в коде
- Пароли в plain text

### ✅ Что делать:
- Все секреты в `.env`
- `.env.example` для шаблона
- Валидация входных данных от внешних API
- SQL injection защита (используй параметризованные запросы)


---

### Миграции
- Все миграции в `migrations/`
- Формат: `NNN_description.sql`
- Используй транзакции: `-- +goose Up`, `-- +goose Down`

---


### 2. Код ревью
- Проверь размеры файлов (<150 строк)
- Проверь количество файлов в папках (<7)
- Убедись в наличии тестов
- Проверь логирование
- Удали временные файлы

### 3. Деплой
- Запусти тесты: `go test ./...`
- Запусти сборку: `go build ./cmd/...`
- Прогони миграции: `goose up`
- Проверь Docker compose: `docker-compose up`

---

## 💡 Лучшие практики

### При добавлении нового функционала
1. ✅ Создай интерфейс в `domain/`
2. ✅ Реализуй в `adapter/` или `usecase/`
3. ✅ Добавь тесты
4. ✅ Добавь логирование
5. ✅ Проверь размеры файлов (<150 строк)
6. ✅ Проверь количество файлов в папке (<7)
7. ✅ Удали временные файлы

### При рефакторинге
1. ✅ Не ломай существующий API
2. ✅ Добавь тесты перед изменением
3. ✅ Делай маленькие коммиты
4. ✅ Обнови документацию

### При работе с внешними API
1. ✅ Всегда проверяй входные данные
2. ✅ Добавь timeout
3. ✅ Логируй запросы/ответы
4. ✅ Обрабатывай rate limiting
5. ✅ Используй retry механизм

---

## 🔍 Troubleshooting

### База данных не доступна
```bash
# Проверь Docker
docker-compose ps

# Проверь переменные окружения
source .env
echo $POSTGRES_HOST

# Проверь подключение
PGPASSWORD=$POSTGRES_PASSWORD psql -h $POSTGRES_HOST -U $POSTGRES_USER -d $POSTGRES_DB
```

### Goose миграции не работают
```bash
# Проверь GOOSE_DBSTRING
echo $GOOSE_DBSTRING

# Используй полный путь
~/go/bin/goose up

# Или установи локально
go install github.com/pressly/goose/v3/cmd/goose@latest
```

### Redis проблемы
```bash
# Проверь Redis
redis-cli ping

# Или через Docker
docker exec -it adscraper_redis redis-cli ping
```

---

## 📚 Полезные ссылки

### Go Backend
- [Go Project Layout](https://github.com/golang-standards/project-layout)
- [Effective Go](https://go.dev/doc/effective_go)
- [Goose Migrations](https://github.com/pressly/goose)

### iOS / SwiftUI
- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui)
- [Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines)
- [Swift API Design Guidelines](https://www.swift.org/documentation/api-design-guidelines/)

---

*Последнее обновление: 2026-01-31*
*Создано Claude Code*
