# План парсера для fhspb.ru

## 🎯 Цель
Парсинг данных Федерации хоккея Санкт-Петербурга для интеграции с существующим проектом HockeyProject.

## 📋 Ограничения
- **Годы рождения**: только 2008 год и старше (не позже 2008)
- **Источник**: https://www.fhspb.ru/
- **Система**: HL.Net (аналогично junior.fhr.ru)

## 🔄 Этапы парсинга

### Этап 1: Парсинг турниров
**Стартовая точка**: `https://www.fhspb.ru/Tournaments?SeasonID=0`

**Извлекаем**:
- ID турнира из ссылки (`TournamentID=6362`)
- Название турнира
- **Год рождения** из названия (регулярное выражение `(\d{4}) г\.р\.`)
- **Фильтр**: только турниры с годом рождения <= 2008
- Даты начала и окончания (`DD.MM.YYYY - DD.MM.YYYY`)
- Сезон (из группировки на странице)
- Количество команд/матчей
- Статус (завершен/текущий)

### Этап 2: Парсинг команд турнира
**URL формирования**: `https://www.fhspb.ru/Teams?TournamentID={TournamentID}`

**Пример**: `https://www.fhspb.ru/Teams?TournamentID=6364`

**Извлекаем для каждой команды**:
- **TeamID** из ссылки (формат UUID: `0545311d-3bec-4fa3-8d54-776e06097488`)
- **Название команды** (например, "Армия СКА 10")
- **Город** (например, "Санкт-Петербург", "Череповец", "Минск")
- **Страна/Локация** (Россия, Беларусь и т.д.)
- **Количество матчей** (например, "Матчей 15")
- **Количество побед** (например, "Побед 13")
- **Количество игроков** (например, "Игроков 27")
- **Количество тренеров** (например, "Тренеров 4")

**URL команды для перехода к составу**:
`https://www.fhspb.ru/Team?TournamentID={TournamentID}&TeamID={TeamID}`

**Пример**: `https://www.fhspb.ru/Team?TournamentID=6364&TeamID=0545311d-3bec-4fa3-8d54-776e06097488`

**Примеры команд из турнира 6364**:
- Армия СКА 10 (Санкт-Петербург, Россия) - 27 игроков
- СКА 10 (Санкт-Петербург, Россия) - 26 игроков
- Северсталь 10 (Череповец, Россия) - 31 игрок
- Динамо Джуниверс 10 (Минск, Беларусь) - 24 игрока

### Этап 3: Парсинг URL игроков из состава команды
**URL страницы команды**: `https://www.fhspb.ru/Team?TournamentID={TournamentID}&TeamID={TeamID}`

**Пример**: `https://www.fhspb.ru/Team?TournamentID=6364&TeamID=0545311d-3bec-4fa3-8d54-776e06097488`

**Цель этапа**: Извлечь ТОЛЬКО URL профилей игроков

**Формат URL игрока**:
```
/Player?TournamentID={TournamentID}&TeamID={TeamID}&PlayerID={PlayerUUID}
```

**Пример**:
```
/Player?TournamentID=6364&TeamID=0545311d-3bec-4fa3-8d54-776e06097488&PlayerID=8b07b9b4-fda2-40bc-bb3b-87546907cb7a
```

**Извлекаем**:
- Полный URL профиля игрока (для последующего перехода)

**Regex для извлечения URL**:
```go
playerURLRegex := regexp.MustCompile(`Player\?TournamentID=\d+&TeamID=[a-f0-9-]{36}&PlayerID=[a-f0-9-]{36}`)
```

**Примеры URL из команды "Армия СКА 10"**:
- `/Player?TournamentID=6364&TeamID=0545311d-3bec-4fa3-8d54-776e06097488&PlayerID=8b07b9b4-fda2-40bc-bb3b-87546907cb7a`
- `/Player?TournamentID=6364&TeamID=0545311d-3bec-4fa3-8d54-776e06097488&PlayerID=a01a1d95-bbc2-466d-87ae-47e36b72a8b1`

### Этап 4: Парсинг профиля игрока (полная информация)
**URL профиля**: `https://www.fhspb.ru/Player?TournamentID={TournamentID}&TeamID={TeamID}&PlayerID={PlayerID}`

**Пример**: `https://www.fhspb.ru/Player?TournamentID=6364&TeamID=0545311d-3bec-4fa3-8d54-776e06097488&PlayerID=f3673116-bd11-4d6b-ab45-287bbd26514f`

**Цель этапа**: Извлечь ВСЮ информацию об игроке

**Извлекаемые поля**:
| Поле | Пример | Описание |
|------|--------|----------|
| ФИО | Балалин Анатолий Витальевич | Полное ФИО (Фамилия Имя Отчество) |
| Амплуа | Нападающий | Полное название (Нападающий/Защитник/Вратарь) |
| Номер | №8 | Игровой номер |
| Роль | А | К - капитан, А - ассистент, пусто - обычный игрок |
| Дата рождения | 08.08.2010 | Формат DD.MM.YYYY |
| Место рождения | Санкт-Петербург | Город рождения |
| Гражданство | Россия | Страна |
| Рост | 173 | В сантиметрах |
| Вес | 74 | В килограммах |
| Хват | Левый | Левый/Правый |
| Школа | Армия СКА, Санкт-Петербург | Поле "Воспитанник" - школа подготовки |

**Структура данных на странице**:
```
### [Балалин Анатолий Витальевич][275]

Нападающий
№8 А

──────────────┬──────────────────────────
Дата рождения │08.08.2010                
──────────────┼──────────────────────────
Возраст       │15 лет 4 мес.             
──────────────┼──────────────────────────
Место рождения│Санкт-Петербург           
──────────────┼──────────────────────────
Гражданство   │Россия                    
──────────────┼──────────────────────────
Рост          │173                       
──────────────┼──────────────────────────
Вес           │74                        
──────────────┼──────────────────────────
Хват          │Левый                     
──────────────┼──────────────────────────
Воспитанник   │Армия СКА, Санкт-Петербург
──────────────┴──────────────────────────
```

**Дополнительно доступно** (опционально):
- Статистика по турниру (И, О, Г, П, +/-, Шт)
- История матчей с результатами
- Ссылка на карьеру игрока

### Этап 5: Парсинг статистики
- Статистика игроков по турниру
- Матчи и результаты
- Детальная статистика (голы, передачи, штрафы и т.д.)

## 📊 Ожидаемые данные
- **Турниры**: ~410 турниров (фильтр по годам рождения)
- **Команды**: ~198 команд в текущем сезоне
- **Игроки**: ~5,068 игроков
- **Регион**: Санкт-Петербург + Ленинградская область

## 🔧 Техническая реализация

### Структура проекта (Clean Architecture + DDD)

```
cmd/
└── fhspb_parser/
    └── main.go                              # Точка входа

internal/
├── app/
│   └── fhspb_parser_app.go                  # Инициализация приложения
│
├── client/
│   └── fhspb/                               # HTTP клиент для fhspb.ru
│       ├── client.go                        # Базовый HTTP клиент
│       ├── tournament.go                    # Парсинг турниров
│       ├── team.go                          # Парсинг команд
│       ├── player.go                        # Парсинг игроков
│       ├── dto.go                           # DTO структуры
│       └── parsing/
│           ├── tournament.go                # Извлечение данных турниров из HTML
│           ├── team.go                      # Извлечение данных команд из HTML
│           └── player.go                    # Извлечение данных игроков из HTML
│
├── service/
│   └── parser/
│       └── fhspb/                           # Сервисный слой парсера
│           ├── service.go                   # Основной сервис (интерфейс)
│           └── orchestrator/
│               ├── service.go               # Оркестратор (DI, зависимости)
│               ├── runner.go                # Запуск процесса парсинга
│               ├── tournament_worker.go     # Воркер для турниров
│               ├── team_worker.go           # Воркер для команд
│               ├── player_worker.go         # Воркер для игроков
│               ├── saver_tournament.go      # Сохранение турниров в БД
│               ├── saver_team.go            # Сохранение команд в БД
│               └── saver_player.go          # Сохранение игроков в БД
│
├── domain/
│   └── (используем существующие: player, team, tournament)
│
└── repository/
    └── postgres/
        └── (используем существующие + новые методы)
```

### Поток данных

```
main.go
    ↓
fhspb_parser_app.go (DI, инициализация)
    ↓
orchestrator/runner.go (запуск)
    ↓
tournament_worker.go → client/fhspb/tournament.go → parsing/tournament.go
    ↓
team_worker.go → client/fhspb/team.go → parsing/team.go
    ↓
player_worker.go → client/fhspb/player.go → parsing/player.go
    ↓
saver_*.go → repository/postgres/*
```

---

## 🗄️ База данных

### Изменения в существующих таблицах

**Новые таблицы НЕ нужны.** Используем существующие: `players`, `teams`, `tournaments`, `player_teams`.

**Миграция для таблицы `players`:**
```sql
-- Добавить поле source для различения источника данных
ALTER TABLE players ADD COLUMN source VARCHAR(50) DEFAULT 'junior.fhr.ru';

-- Добавить внешний ID источника (для дедупликации)
ALTER TABLE players ADD COLUMN external_id VARCHAR(100);

-- Добавить гражданство и роль
ALTER TABLE players ADD COLUMN citizenship VARCHAR(100);
ALTER TABLE players ADD COLUMN role VARCHAR(10);  -- К/А/пусто

-- Индексы
CREATE INDEX idx_players_source ON players(source);
CREATE UNIQUE INDEX idx_players_external_source ON players(external_id, source);
```

**Зачем поле `source`?**
- Различать откуда пришёл игрок: `junior.fhr.ru` или `fhspb.ru`
- Дедупликация: один игрок может быть в обоих источниках
- Возможность обновлять данные только из конкретного источника

**Зачем поле `external_id`?**
- Хранить PlayerID из fhspb.ru (UUID)
- Избежать дубликатов при повторном парсинге
- Быстрый поиск по внешнему ID

---

## 🏗️ Domain слой

### Расширение существующей сущности `player.Entity`

**Новые сущности НЕ нужны.** Расширяем существующую:

```go
// internal/domain/player/entity.go
type Entity struct {
    ID             int64
    FirstName      string
    LastName       string
    MiddleName     string      // отчество
    BirthDate      time.Time
    BirthPlace     string      // место рождения
    Citizenship    string      // гражданство (НОВОЕ)
    Position       string      // амплуа
    Number         int         // номер
    Role           string      // К/А/пусто (НОВОЕ)
    Height         int
    Weight         int
    Stick          string      // хват
    School         string      // школа/воспитанник
    Source         string      // источник данных (НОВОЕ)
    ExternalID     string      // ID в источнике (НОВОЕ)
    // ...существующие поля
}
```

---

## 📦 Repository слой

### Новые методы в `player.Repository`

```go
// internal/domain/player/repository.go
type Repository interface {
    // Существующие методы
    Create(ctx context.Context, player *Entity) error
    Update(ctx context.Context, player *Entity) error
    GetByID(ctx context.Context, id int64) (*Entity, error)
    
    // НОВЫЕ методы для fhspb парсера
    GetByExternalID(ctx context.Context, externalID, source string) (*Entity, error)
    ExistsByExternalID(ctx context.Context, externalID, source string) (bool, error)
    Upsert(ctx context.Context, player *Entity) error  // создать или обновить
}
```

**Зачем `GetByExternalID`?**
- Проверить, есть ли уже игрок с таким PlayerID из fhspb.ru
- Избежать дубликатов при повторном парсинге

**Зачем `Upsert`?**
- Если игрок есть - обновить данные
- Если нет - создать нового
- Один метод вместо проверки + create/update

---

## ⚡ Параллельная обработка (Worker Pool)

### Схема воркеров

```
                    ┌─────────────────┐
                    │  Orchestrator   │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │ Worker 1 │  │ Worker 2 │  │ Worker 3 │
        └────┬─────┘  └────┬─────┘  └────┬─────┘
             │             │             │
             ▼             ▼             ▼
        Tournament    Tournament    Tournament
            │             │             │
            ▼             ▼             ▼
          Teams         Teams         Teams
            │             │             │
            ▼             ▼             ▼
        Players       Players       Players
```

### Конфигурация

```go
type Config struct {
    TournamentWorkers int           // 3-5 параллельных турниров
    TeamWorkers       int           // 5-10 параллельных команд
    PlayerWorkers     int           // 10-20 параллельных игроков
    RequestDelay      time.Duration // задержка между запросами (100-200ms)
}
```

**Почему важна задержка (`RequestDelay`)?**
- Не нагружать сервер fhspb.ru
- Избежать блокировки по IP
- Вежливый парсинг

---

## 📊 Резюме изменений

| Компонент | Новое? | Что делаем |
|-----------|--------|------------|
| Таблицы БД | Нет | Добавляем поля `source`, `external_id`, `citizenship`, `role` |
| Domain Entity | Нет | Расширяем `player.Entity` новыми полями |
| Repository | Частично | Добавляем методы `GetByExternalID`, `ExistsByExternalID`, `Upsert` |
| Client | Да | Новый `internal/client/fhspb/` |
| Service | Да | Новый `internal/service/parser/fhspb/` |
| App | Да | Новый `internal/app/fhspb_parser_app.go` |
| CMD | Да | Новый `cmd/fhspb_parser/main.go` |
| Workers | Да | Параллельная обработка с rate limiting |

---

## ✅ TODO

### Подготовка
- [ ] Создать миграцию для новых полей в таблице `players`
- [ ] Расширить `player.Entity` новыми полями
- [ ] Добавить новые методы в `player.Repository`
- [ ] Реализовать новые методы в `postgres/player`

### HTTP клиент (`internal/client/fhspb/`)
- [ ] Создать базовый HTTP клиент
- [ ] Реализовать парсинг турниров
- [ ] Реализовать парсинг команд
- [ ] Реализовать парсинг URL игроков
- [ ] Реализовать парсинг профиля игрока

### Сервисный слой (`internal/service/parser/fhspb/`)
- [ ] Создать оркестратор
- [ ] Реализовать воркеры для турниров
- [ ] Реализовать воркеры для команд
- [ ] Реализовать воркеры для игроков
- [ ] Реализовать сохранение в БД

### Приложение
- [ ] Создать `fhspb_parser_app.go`
- [ ] Создать `cmd/fhspb_parser/main.go`
- [ ] Добавить задачу в `Taskfile.yml`

### Тестирование
- [ ] Тестирование парсинга на реальных данных
- [ ] Верификация сохранённых данных
- [ ] Проверка дедупликации

---

## 📝 Заметки

- Система HL.Net - знакомая структура
- Четкая группировка по годам рождения
- Региональная специфика (СПб)
- Большой объем исторических данных (2009-2025)
- Статистика игроков будет парситься отдельным процессом (`fhspb_stats_parser`)
