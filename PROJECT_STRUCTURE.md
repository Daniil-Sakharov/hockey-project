# 🏗️ Структура проекта Hockey Stats Parser

## Общая архитектура

Проект построен на принципах **Clean Architecture** с **Dependency Injection**:
- Зависимости направлены внутрь (к domain слою)
- Бизнес-логика изолирована от внешних фреймворков
- Легко тестировать и расширять
- Модульный монолит: parser и bot - отдельные процессы, общая кодовая база

## 📁 Структура директорий

```
HockeyProject/
│
├── cmd/                           # Точки входа приложений
│   ├── parser/                    # Процесс парсера
│   │   └── main.go               # Инициализация DI, запуск cron
│   └── bot/                       # Процесс Telegram бота
│       └── main.go               # Инициализация DI, запуск бота
│
├── internal/                      # Приватный код приложения
│   │
│   ├── domain/                    # СЛОЙ 1: Бизнес-сущности и интерфейсы
│   │   ├── entity/               # Бизнес-модели (чистые структуры)
│   │   │   ├── player.go         # Сущность игрока
│   │   │   ├── game.go           # Сущность матча
│   │   │   └── game_stats.go     # Статистика игрока в матче
│   │   │
│   │   └── repository/           # Интерфейсы репозиториев (контракты)
│   │       ├── player.go         # interface PlayerRepository
│   │       ├── game.go           # interface GameRepository
│   │       └── stats.go          # interface StatsRepository
│   │
│   ├── usecase/                   # СЛОЙ 2: Бизнес-логика (use cases)
│   │   ├── player/               # Use cases для работы с игроками
│   │   │   ├── create.go         # Создание игрока
│   │   │   ├── search.go         # Поиск игроков
│   │   │   └── update.go         # Обновление данных
│   │   │
│   │   ├── stats/                # Use cases для статистики
│   │   │   ├── aggregate.go      # Агрегация статистики
│   │   │   └── calculate.go      # Расчет показателей
│   │   │
│   │   └── scraping/             # Use cases для парсинга
│   │       ├── orchestrator.go   # Оркестрация всех парсеров
│   │       └── processor.go      # Обработка спаршенных данных
│   │
│   ├── delivery/                  # СЛОЙ 3: Точки входа (handlers)
│   │   ├── telegram/             # Telegram bot handlers
│   │   │   ├── handler.go        # Главный handler
│   │   │   ├── commands/         # Обработчики команд
│   │   │   │   ├── start.go      # /start
│   │   │   │   ├── help.go       # /help
│   │   │   │   └── search.go     # /search, /findbyage, etc.
│   │   │   │
│   │   │   ├── keyboard/         # Inline клавиатуры
│   │   │   │   └── builder.go    # Построение клавиатур
│   │   │   │
│   │   │   └── formatter/        # Форматирование ответов
│   │   │       └── player.go     # Красивый вывод данных игрока
│   │   │
│   │   └── cron/                 # Cron jobs
│   │       └── scraper_job.go    # Job для запуска парсинга
│   │
│   ├── infrastructure/            # СЛОЙ 4: Внешние сервисы и адаптеры
│   │   │
│   │   ├── repository/           # Реализации репозиториев (PostgreSQL)
│   │   │   ├── player_pg.go      # PlayerRepository с sqlx
│   │   │   ├── game_pg.go        # GameRepository с sqlx
│   │   │   └── stats_pg.go       # StatsRepository с sqlx
│   │   │
│   │   ├── scraper/              # Реализации парсеров
│   │   │   ├── scraper.go        # interface Scraper (контракт)
│   │   │   ├── manager.go        # Менеджер всех парсеров
│   │   │   │
│   │   │   ├── sites/            # Конкретные парсеры сайтов
│   │   │   │   ├── site1/
│   │   │   │   │   ├── scraper.go    # Реализация парсера
│   │   │   │   │   └── mapper.go     # Маппинг в domain entity
│   │   │   │   │
│   │   │   │   └── site2/
│   │   │   │       ├── scraper.go
│   │   │   │       └── mapper.go
│   │   │   │
│   │   │   └── common/           # Общий код для парсеров
│   │   │       ├── client.go     # HTTP клиент с retry
│   │   │       └── limiter.go    # Rate limiter
│   │   │
│   │   ├── telegram/             # Telegram bot client
│   │   │   └── bot.go            # Обертка над tgbotapi
│   │   │
│   │   └── scheduler/            # Планировщик задач
│   │       └── cron.go           # Обертка над robfig/cron
│   │
│   ├── config/                    # Конфигурация приложения
│   │   ├── config.go             # Структура конфига
│   │   └── loader.go             # Загрузка из .env
│   │
│   └── container/                 # DI контейнер
│       ├── container.go          # Главный контейнер
│       ├── parser.go             # DI для parser процесса
│       └── bot.go                # DI для bot процесса
│
├── pkg/                           # Платформенная библиотека (ваша готовая)
│   ├── logger/                   # Zap logger (готовый)
│   ├── closer/                   # Graceful shutdown (готовый)
│   └── migrator/                 # Goose migrator (готовый)
│
├── migrations/                    # SQL миграции (goose)
│   ├── 00001_initial_schema.sql
│   ├── 00002_add_game_stats.sql
│   └── ...
│
├── configs/                       # Конфигурационные файлы
│   └── .env.example              # Пример .env файла
│
├── docker/                        # Docker конфигурация
│   ├── Dockerfile.parser         # Dockerfile для парсера
│   ├── Dockerfile.bot            # Dockerfile для бота
│   └── docker-compose.yml        # Compose для всего стека
│
├── .env                          # Секреты (НЕ коммитить!)
├── .env.example                  # Пример секретов
├── .gitignore
├── go.mod
├── go.sum
├── Makefile                      # Утилиты для разработки
└── README.md
```

---

## 🔍 Детальное описание слоев

### 1️⃣ Domain Layer (internal/domain/)

**Цель:** Чистая бизнес-логика без зависимостей от внешнего мира

#### entity/
- **Что:** Бизнес-сущности (Player, Game, GameStats)
- **Правила:** 
  - Только структуры данных и методы работы с ними
  - Никаких зависимостей от БД, фреймворков
  - Теги для БД (db:"...") допустимы для удобства sqlx
- **Пример:** `Player` содержит ID, имя, дату рождения, методы валидации

#### repository/
- **Что:** Интерфейсы (контракты) для работы с данными
- **Правила:**
  - Только интерфейсы, никаких реализаций
  - Методы в терминах бизнес-логики (Create, FindByName, Update)
  - Принимают и возвращают domain entities
- **Зачем:** Бизнес-логика не зависит от конкретной БД

---

### 2️⃣ Use Case Layer (internal/usecase/)

**Цель:** Реализация бизнес-сценариев приложения

#### player/
- **Что:** Use cases для работы с игроками
- **create.go:** Создание нового игрока с валидацией
- **search.go:** Поиск по имени, возрасту, команде
- **update.go:** Обновление данных игрока
- **Зависимости:** Только domain/repository интерфейсы

#### stats/
- **Что:** Работа со статистикой
- **aggregate.go:** Агрегация статистики за сезон
- **calculate.go:** Расчет показателей (points per game, и т.д.)

#### scraping/
- **Что:** Оркестрация процесса парсинга
- **orchestrator.go:** Управляет запуском парсеров, обработкой ошибок
- **processor.go:** Обрабатывает спаршенные данные, сохраняет в БД
- **Зависимости:** repository интерфейсы, scraper интерфейс

**Правила:**
- Use case не знает откуда данные (БД, API, файл)
- Принимает зависимости через конструктор (DI)
- Возвращает domain entities или ошибки

---

### 3️⃣ Delivery Layer (internal/delivery/)

**Цель:** Точки входа в приложение (HTTP, Telegram, Cron)

#### telegram/
- **handler.go:** Главный обработчик обновлений от Telegram
- **commands/:** Обработчики каждой команды бота
  - Получает запрос от пользователя
  - Вызывает нужный use case
  - Форматирует ответ
- **keyboard/:** Построение inline клавиатур
- **formatter/:** Красивое форматирование данных для вывода

#### cron/
- **scraper_job.go:** Cron job для запуска парсинга по расписанию
  - Вызывает scraping use case
  - Логирует результаты

**Правила:**
- Не содержит бизнес-логики (только вызовы use cases)
- Зависит от use cases через интерфейсы
- Отвечает за формат данных (JSON, Telegram markup)

---

### 4️⃣ Infrastructure Layer (internal/infrastructure/)

**Цель:** Реализации интерфейсов для внешних сервисов

#### repository/
- **Что:** Реализации domain/repository интерфейсов для PostgreSQL
- **player_pg.go:** Реализует PlayerRepository используя sqlx
  - SQL запросы
  - Маппинг БД ↔ domain entity
- **Зачем:** Изолирует БД логику от бизнес-логики

#### scraper/
- **scraper.go:** Интерфейс Scraper (контракт для всех парсеров)
- **manager.go:** Управляет всеми парсерами
  - Запускает их по очереди или параллельно
  - Собирает результаты
  - Обрабатывает ошибки
  
- **sites/site1/:**
  - **scraper.go:** Реализация парсинга конкретного сайта (Colly)
  - **mapper.go:** Преобразование спаршенных данных в domain entities
  
- **common/:**
  - **client.go:** HTTP клиент с retry логикой
  - **limiter.go:** Rate limiter (не больше N запросов в секунду)

#### telegram/
- **bot.go:** Обертка над tgbotapi для удобной работы

#### scheduler/
- **cron.go:** Обертка над robfig/cron для удобной работы

**Правила:**
- Реализует интерфейсы из domain/
- Может зависеть от внешних библиотек
- Знает о деталях (SQL, HTTP, Telegram API)

---

## 🔌 DI Container (internal/container/)

**Цель:** Центральное место для создания и связывания всех зависимостей

### container.go
- Главный контейнер со всеми зависимостями
- Создает все компоненты в правильном порядке
- Инжектит зависимости

### parser.go
- Специфичная инициализация для parser процесса
- Создает:
  - Подключение к БД
  - Репозитории
  - Use cases
  - Парсеры
  - Cron scheduler
  - Closer для graceful shutdown

### bot.go
- Специфичная инициализация для bot процесса
- Создает:
  - Подключение к БД
  - Репозитории
  - Use cases
  - Telegram bot
  - Handlers
  - Closer для graceful shutdown

**Зачем:**
- Единое место управления зависимостями
- Легко менять реализации
- Легко тестировать (mock'и)

---

## ⚙️ Config (internal/config/)

### config.go
Структура со всеми настройками приложения:
- Database (URL, pool size)
- Telegram (bot token)
- Scraping (interval, concurrent scrapers)
- Logging (level)

### loader.go
- Загрузка из .env файла
- Валидация обязательных полей
- Значения по умолчанию

---

## 📦 Platform Library (pkg/)

**Ваша готовая библиотека:**

### logger/
- Настроенный Zap logger
- Используется везде для логирования

### closer/
- Graceful shutdown
- Регистрация функций для закрытия ресурсов
- Обработка сигналов ОС (SIGTERM, SIGINT)

### migrator/
- Обертка над goose
- Запуск миграций БД при старте
- Rollback при необходимости

---

## 🗄️ Migrations (migrations/)

Goose миграции для PostgreSQL:
- Версионирование схемы БД
- Up/Down миграции
- История изменений

Формат: `00001_description.sql`

---

## 🐳 Docker (docker/)

### Dockerfile.parser
- Образ для parser процесса
- Multi-stage build (builder + runtime)
- Chromium НЕ нужен (пока только Colly)

### Dockerfile.bot
- Образ для bot процесса
- Multi-stage build

### docker-compose.yml
Запуск всего стека:
- PostgreSQL
- Parser
- Bot
- pgAdmin (опционально, для dev)

---

## 🔄 Поток данных

### Парсинг (Parser процесс):
```
Cron Scheduler
    ↓
Cron Job (delivery/cron)
    ↓
Scraping Use Case (usecase/scraping)
    ↓
Scraper Manager (infrastructure/scraper)
    ↓
Site Scrapers (infrastructure/scraper/sites)
    ↓ (спаршенные данные)
Mapper (преобразование в domain entities)
    ↓
Repository (infrastructure/repository)
    ↓
PostgreSQL
```

### Telegram Bot (Bot процесс):
```
Telegram API
    ↓
Bot Client (infrastructure/telegram)
    ↓
Handler (delivery/telegram)
    ↓
Commands (delivery/telegram/commands)
    ↓
Use Case (usecase/player, usecase/stats)
    ↓
Repository (infrastructure/repository)
    ↓
PostgreSQL
    ↓ (данные)
Formatter (delivery/telegram/formatter)
    ↓
Telegram API → User
```

---

## 🎯 Принципы разработки

### Dependency Rule
Зависимости направлены только внутрь:
```
delivery → usecase → domain
infrastructure → domain
```

Domain ни от чего не зависит!

### Интерфейсы
- Use case зависит от интерфейса репозитория (domain/repository)
- Конкретная реализация (infrastructure/repository) инжектится через DI

### Тестирование
- Domain: легко тестировать (нет зависимостей)
- Use case: тестируем с mock репозиториями
- Infrastructure: интеграционные тесты с тестовой БД

---

## 📝 Пример: Как это работает вместе

### Сценарий: Пользователь ищет игрока через бота

1. **Delivery:** Telegram handler получает `/search Овечкин`
2. **Delivery → Use Case:** Вызывает `playerUseCase.Search("Овечкин")`
3. **Use Case → Repository:** Вызывает `playerRepo.FindByName("Овечкин")`
4. **Repository (Infrastructure):** Выполняет SQL запрос через sqlx
5. **PostgreSQL:** Возвращает данные
6. **Repository → Use Case:** Возвращает `[]entity.Player`
7. **Use Case → Delivery:** Возвращает найденных игроков
8. **Delivery:** Форматирует и отправляет в Telegram

### Сценарий: Ежедневный парсинг

1. **Cron Scheduler:** Наступает 2:00 AM
2. **Cron Job (Delivery):** Вызывает `scrapingUseCase.RunDaily()`
3. **Use Case:** Вызывает `scraperManager.ScrapeAll()`
4. **Scraper Manager:** Запускает все зарегистрированные парсеры
5. **Site Scraper:** Парсит сайт через Colly
6. **Mapper:** Преобразует в `entity.Player`
7. **Use Case → Repository:** Сохраняет через `playerRepo.CreateOrUpdate()`
8. **Repository:** Сохраняет в PostgreSQL
9. **Logger:** Логирует результаты

---

## ✅ Преимущества такой структуры

1. **Тестируемость:** Каждый слой легко тестировать независимо
2. **Гибкость:** Легко менять БД, парсеры, bot framework
3. **Масштабируемость:** Легко добавлять новые use cases, парсеры
4. **Понятность:** Четкое разделение ответственности
5. **Обучение:** Идеальна для изучения Clean Architecture

---

## 🚀 Следующие шаги

1. Покажите мне сайты для парсинга - я помогу спроектировать парсеры
2. Инициализируем проект (go.mod, структура директорий)
3. Настроим БД и миграции
4. Создадим domain entities
5. Реализуем первый парсер
6. Настроим DI контейнер
7. Запустим parser процесс
8. Создадим Telegram бота
9. Задеплоим в Docker

**Готовы начать? Покажите первый сайт для парсинга! 🏒**

